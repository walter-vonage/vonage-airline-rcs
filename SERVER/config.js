const data = {
    VERSION: '0.0.1',
    FROM: 'WalterRodriguez',
    API_KEY: '',
    API_SECRET: '',
    JWT: '',
    SERVER: process.env.VCR_INSTANCE_PUBLIC_URL,
    WEBHOOK: process.env.VCR_INSTANCE_PUBLIC_URL
}

module.exports = {
    data
}